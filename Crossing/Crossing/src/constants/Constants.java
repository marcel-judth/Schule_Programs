package constants;

public interface Constants {
    int CAR_SPAWN_DELAY = 1000;
    double CAR_SPEED = 0.2,
            CAR_SIZE_FACTOR = 0.08,
            CAR_WIDTH = 754 * CAR_SIZE_FACTOR,
            CAR_HEIGHT = 400 * CAR_SIZE_FACTOR,
            CURVE_FACTOR = 1.3;
}
